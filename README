TODO
----

Convert Static File Server to use 
node-static: RFC2616 compliant HTTP static-file server module, with built-in caching

Refactor Methods for working with games and users into objects

Consistant file nameing scheme

Documentation standard choice

Event Refactoring (possibly make all module extend a base object that extends event emitter)

Replace client side 'magic method calling' with true event listeners

Abstract out the socket (or write wrappers but keep socket in place)

Redefine users to be objects with properties and methods interacting with database storage layer.

Abstract user disconnection from the matchmaker module

Add mongodb database storage layer

Abstract user disconnection from the gamestate/gameaction **FIXED**


Bugs
----
When user disconnects after joining game but before opponent joins, crashes server. **FIXED**
